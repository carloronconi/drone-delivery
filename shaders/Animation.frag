#version 450#extension GL_ARB_separate_shader_objects : enablelayout(location = 0) in vec3 fragPos;layout(location = 0) out vec4 outColor;layout(binding = 0) uniform UniformBufferObject {	float visible;	mat4 mvpMat;	vec3 offset;	float time;} ubo;const vec4 color = vec4(0.3, 0.3, 0.3, 1); // solid grayvoid main() {	// change to also take transparency from texture (w) to allow transparent PNGs	outColor = color;	// output color}